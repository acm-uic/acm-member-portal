/**
 * Phase 6 stub — Phase 11 fills in the nightly alumni digest (candidate
 * computation, officer email lookup, SMTP send). Required by the worker
 * dispatcher's dynamic import so the Phase 6 graph compiles end-to-end.
 */
export async function runAlumniDigest(): Promise<void> {
	// Phase 11: compute candidates, look up officers, send digest email.
}
